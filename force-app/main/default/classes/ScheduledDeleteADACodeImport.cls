global class ScheduledDeleteADACodeImport implements Schedulable { 
    
    global void execute(SchedulableContext SC) { 
      /*  String s = 'SELECT Id FROM ADA_Code_Import__c';
        BatchDelete bd = new BatchDelete();
        bd.query = s;
        Database.executeBatch(bd);*/
    }
}